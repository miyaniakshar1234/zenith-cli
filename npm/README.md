# Zenith CLI (NPM Wrapper)

This is the Node.js wrapper for [Zenith CLI](https://github.com/miyaniakshar1234/zenith-cli), ensuring easy installation in restricted environments.

## 📦 Installation

```bash
# Run instantly (No install needed)
npx @miyaniakshar/zenith-cli

# OR Install globally
npm install -g @miyaniakshar/zenith-cli
```

## 🚀 Usage
```bash
zenith-cli
```

*Note: This package bundles the Rust binary, so it works offline and bypasses GitHub download restrictions.*
